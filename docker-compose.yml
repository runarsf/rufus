version: "3"

services:
  rufus:
    image: rufus
    hostname: rufus
    container_name: rufus
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      TZ: Europe/Oslo
      DOCKER_MODE: 'true'
      GIT_PYTHON_REFRESH: quiet
    volumes:
      - "./src:/app"
      #- ./logs:/app/logs
    network_mode: host
    #ports:
      #- 8080:8080
