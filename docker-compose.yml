version: "3"

services:
  rufus:
    container_name: rufus
    hostname: rufus
    restart: unless-stopped
    image: rufus
    build:
      context: .
    environment:
        - BOT_TOKEN=$BOT_TOKEN
        - WOLFRAM_API_KEY=$WOLFRAM_API_KEY
    ports:
      - 8080:8080
